# Extra autostart processes — only add things NOT in the default autostart
# Default autostart is at: ~/.local/share/omarchy/default/hypr/autostart.conf

# Cursor theme (custom — not in default)
exec-once = hyprctl setcursor synthwave-night 24
